dependencies {
    compile "com.github.briandilley.jsonrpc4j:jsonrpc4j:1.1"
    compile "javax.inject:javax.inject:1"

    // Temporary - JSON module shouldn't have dependency on Spring, but for now we need it
    // for the PeerService
    compile "org.springframework.boot:spring-boot-starter-websocket:1.3.0.M2"

    testCompile("junit:junit:4.11")
}

sourceCompatibility = 1.7
